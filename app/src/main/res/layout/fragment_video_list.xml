<FrameLayout

    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:draggable_panel="http://schemas.android.com/apk/lib"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- блок поиска -->
        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#ffff782b" >

            <TextView
                android:text="@string/search"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <EditText
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/searchTextEdit"
                android:layout_marginLeft="8dp"
                android:singleLine="true" />

        </LinearLayout>
        <!-- прогресс загрузки -->
        <ProgressBar
            android:id="@+id/video_list_progress"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            />

        <!-- список видео -->

        <ListView
            android:id="@+id/video_list_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/hint_foreground_material_dark"
            android:tag="list"/>

    </LinearLayout>

    <!-- Layout всплывающей панели -->
    <com.github.pedrovgs.DraggablePanel
        android:id="@+id/draggable_panel"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        draggable_panel:x_scale_factor="2.5"
        draggable_panel:y_scale_factor="2.5"
        draggable_panel:top_fragment_margin_right="10dp"
        draggable_panel:top_fragment_margin_bottom="10dp"
        draggable_panel:enable_horizontal_alpha_effect="false"/>

</FrameLayout>